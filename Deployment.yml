---
- hosts: webserver
  become: true
  become_user: root
 
  tasks:
  - name: install docker
    yum: name=docker state=present
 
  - name: start docker
    service: name=docker state=started

  - name: run the image to create container
    command: docker pull bhumi0407/myproject30
    
